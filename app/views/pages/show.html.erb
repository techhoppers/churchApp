<div class="container">

        <!-- Marketing Icons Section -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">
                  <%= @page.title.camelize if @page.title %>
                </h1>
            </div>
            <div class="col-lg-12">
                <div class="panel">
                    <div class="panel-body">
                      <%= @page.description.html_safe if @page.description %>
                    </div>
                </div>
            </div>
        </div>
        <!-- /.row -->
    </div>